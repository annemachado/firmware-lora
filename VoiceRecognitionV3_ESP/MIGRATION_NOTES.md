# Migração para o repositório separado

- Esta biblioteca é o mesmo driver que estava em `libraries/VoiceRecognitionV3_ESP`.
- A API pública (classe `VR_ESP` e métodos) permanece idêntica.
- Para usar no projeto principal, remova a pasta antiga e adicione este repositório
  como biblioteca na pasta `Arduino/libraries` ou via ZIP.
